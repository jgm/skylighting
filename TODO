_ test against h-k for correctness
  _ life.lua

    - <span class="kw">function</span> delay<span class="ot">()</span> <span class="do">-- NOTE: SYSTEM-DEPENDENT, adjust as necessary</span>
    + <span class="kw">function</span> delay<span class="ot">()</span> <span class="do">-- </span><span class="al">NOTE</span><span class="do">: SYSTEM-DEPENDENT, adjust as necessary</span>
        <span class="kw">for</span> i<span class="ot">=</span><span class="dv">1</span><span class="ot">,</span><span class="dv">10000</span> <span class="kw">do</span> <span class="kw">end</span>
        <span class="do">-- local i=os.clock()+1 while(os.clock()</span><span class="kw">&lt;i</span><span class="ot">) do end</span>
    - <span class="kw">end</span>
    + <span class="do">end</span>

_ make insensitive attribute work with StringDetect

_ set column = 0 for regexes starting with ^

_ fix FromJSON instance for Style, Color, and
  add a function to read a KDE theme as a style
  (also add the standard ones to the library)

_ make faster
  _ can we compile some modules with optimizations, others without?
  _ benchmark and profile
  _ use Text instead of String
  _ use Set for Char matchers (parameterize WordSet?)
  _ improve normalize fn?
  _ avoid creating empty tokens?

_ proper haddock documentation

_ proper README.md

_ proper tests
